<dev class="flash">
  <%= flash[:destroy] %>
</dev>
<h1>Books</h1>

<div class="table">
  <div class="thread">
    <p class="thread titlename">Title</p>
    <p class="thread bodyname">Body</p>
  </div>
  <div class="tb">
    <% @book.each do |book| %>
      <div class="tc">
        <p class="element titlename"><%= book.title %></p>
        <p class="element bodyname"><%= book.body %></p>
        <p class="element"><%= link_to "Show", book_path(book.id) %></p>
        <p class="element"><%= link_to "Edit", edit_book_path(book.id) %></p>
        <p class="element"><%= link_to "Destroy", destroy_book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></p>
      </div>
    <% end %>
  </div>
</div>

<h2>New book</h2>


<% flash.each do |f| %>
  <div class="errorbrank">
  <%= content_tag :div, "2 errors prohibited this book from being saved:", class: "flash-alert" %>
  <ul class="errorb">
    <li class="errorlist">Title can'be blank</li>
    <li class="errorlist">Body can't be blank</li>
  </ul>
  </div>
<% end %>

<%= form_with model: Book.new do |f| %>

  <div class="field">
  <h4>Title</h4>
  <%= f.text_field :title %>
  </div>
  <div class="field">
  <h4>Body</h4>
  <%= f.text_area :body %>
  </div>
  <div class="action">
  <%= f.submit 'Create Book' %>
  </div>
<% end %>