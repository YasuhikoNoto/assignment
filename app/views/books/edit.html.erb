<h1>Editing Book</h1>
<%= form_with model: @book, url:update_book_path(@book.id), method: :patch do |f| %>
<% flash.each do |f| %>
  <div class="errorbrank">
  <%= content_tag :div, "2 errors prohibited this book from being saved:", class: "flash-alert" %>
  <ul class="errorb">
    <li class="errorlist">Title can'be blank</li>
    <li class="errorlist">Body can't be blank</li>
  </ul>
  </div>
<% end %>

<div class="field">
  <h4>Title</h4>
  <%= f.text_field :title %>
</div>
<div class="field">
  <h4>Body</h4>
  <%= f.text_area :body %>
</div>
<div class="action">
  <%= f.submit 'Update Book' %>
</div>

<% end %>

<span><%= link_to "Show", book_path(@book.id) %> | </span>
<span><%= link_to "Back", index_book_path %></span>